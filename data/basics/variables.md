# Variables in Rust

Variables are immutable by default. Use `mut` to make them mutable.

## Example
```rust
fn main() {
    let x = 5;
    let mut y = 10;
    y = 20;
}
Practice

Create a mutable variable and update its value.var
